<div class="c-time-entry__left-side">
  {{one-way-input
      class="c-time-entry-form__input"
      placeholder="What were you doing?"
      value=name
      update=(action (mut name))
      onenter=(perform submitFormTask)
  }}
</div>

<div class="c-time-entry__right-side">
  <button
    class="c-button c-button--danger c-time-entry-form__button"
    onclick={{action deleteEntry}}
  >
    Delete
  </button>

  <button
    class="c-button c-time-entry-form__button"
    onclick={{action closeForm}}
  >
    Cancel
  </button>

  <button
    class="c-button c-button--primary c-time-entry-form__button {{if isDisabled 'is-disabled'}}"
    onclick={{perform submitFormTask}}
  >
    Save
  </button>
</div>
