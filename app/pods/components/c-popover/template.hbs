{{#tether-dialog
    container-class="c-popover"
    target=target
    attachment=attachment
    targetAttachment=targetAttachment
    constraints=constraints
    offset=offset
    clickOutsideToClose=clickOutsideToClose
    hasOverlay=false
    close=this.attrs.close
}}
  <div class="c-popover__arrow"></div>
  {{yield}}
{{/tether-dialog}}