{{#each sortedTimeEntries as |timeEntry|}}
  {{#if timeEntry.isStopped}}
    {{time-entry
        timeEntry=timeEntry
        continueEntry=(action continueTimeEntry timeEntry)
    }}
  {{else}}
    {{current-entry
        timeEntry=timeEntry
        stopEntry=(action stopTimeEntry timeEntry)
    }}
  {{/if}}
{{/each}}
