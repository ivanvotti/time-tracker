{{#each sortedTimeEntries as |timeEntry|}}
  {{#if timeEntry.isStopped}}
    {{time-entry timeEntry=timeEntry}}
  {{else}}
    {{current-entry
        timeEntry=timeEntry
        stopTimer=(action this.attrs.stopTimeEntry timeEntry)
    }}
  {{/if}}
{{/each}}
