{{#each-in
    (group-by "startedAtDisplay" sortedTimeEntries)
    as |entryListTitle timeEntries|
}}
  <div class="c-entry-list">
    <div class="c-entry-list__header">
      <div class="c-entry-list__title">
        {{entryListTitle}}
      </div>
    </div>

    {{#each timeEntries as |timeEntry|}}
      {{entry-item
          timeEntry=timeEntry
          tags=tags
          restartEntry=(action restartEntry timeEntry)
          updateEntry=(action updateEntry timeEntry)
          deleteEntry=(action deleteEntry timeEntry)
          stopEntry=(action stopEntry timeEntry)
      }}
    {{/each}}
  </div>
{{/each-in}}
